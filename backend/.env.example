# SkillMatch AI Backend Configuration
# 
# ⚠️  SECURITY WARNING: This is a template file with placeholder values
# ⚠️  Copy this to .env and replace with actual values
# ⚠️  NEVER commit .env files with real secrets to version control!
# 
# To use this file:
# 1. Copy: cp .env.example .env
# 2. Replace all placeholder values with real credentials
# 3. Ensure .env is in .gitignore (it should be!)
#

# Database
DATABASE_URL=postgresql+asyncpg://YOUR_DB_USER:YOUR_DB_PASSWORD@YOUR_DB_HOST:5432/YOUR_DB_NAME
DATABASE_POOL_SIZE=20
DATABASE_MAX_OVERFLOW=30

# JWT Authentication - CHANGE THESE IN PRODUCTION!
JWT_SECRET_KEY=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_IN_PRODUCTION
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# DigitalOcean Spaces Configuration - Replace with your actual keys
DO_SPACES_ACCESS_KEY=YOUR_DO_SPACES_ACCESS_KEY
DO_SPACES_SECRET_KEY=YOUR_DO_SPACES_SECRET_KEY
DO_SPACES_BUCKET_NAME=your-bucket-name
DO_SPACES_REGION=your-region
DO_SPACES_ENDPOINT=https://your-bucket.your-region.digitaloceanspaces.com
DO_SPACES_CDN_ENDPOINT=https://your-bucket.your-region.cdn.digitaloceanspaces.com

# File Upload Settings
MAX_FILE_SIZE_MB=25
ALLOWED_FILE_TYPES=pdf,docx,doc,txt
UPLOAD_FOLDER=resumes

# Redis Configuration (for caching and background tasks)
REDIS_URL=redis://localhost:6379/0

# AI/ML Configuration
OPENAI_API_KEY=sk-YOUR_OPENAI_API_KEY_HERE
SENTENCE_TRANSFORMER_MODEL=all-MiniLM-L6-v2

# Email Configuration (for notifications) - Replace with your email settings
SMTP_HOST=smtp.your-email-provider.com
SMTP_PORT=587
SMTP_USERNAME=your_actual_email@yourdomain.com
SMTP_PASSWORD=your_actual_app_password
EMAIL_FROM=noreply@yourdomain.com

# Application Settings
APP_NAME=SkillMatch AI
APP_VERSION=1.0.0
DEBUG=True
ENVIRONMENT=development

# CORS Settings
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://yourdomain.com

# Rate Limiting
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000

# Monitoring
SENTRY_DSN=your_sentry_dsn_if_using_sentry

# Security
BCRYPT_ROUNDS=12
SECURE_COOKIES=False  # Set to True in production with HTTPS
